# Offline training/export dependencies for Module C (train_and_export.py).
# NOT required by the Flask app — the dashboard only reads the precomputed
# JSON/model artifacts these produce. Install separately:
#   pip install -r module_c/requirements-train.txt
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11
xgboost>=2.0
